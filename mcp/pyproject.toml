[project]
name = "hermes-mcp-server"
version = "0.1.0"
description = "HVE Hermes MCP Server — exposes Hermes CFO capabilities to Copilot Studio"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.0",
    "fastmcp>=3.4,<4",
    "websockets==15.0.1",
]
