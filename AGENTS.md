# Repository Retirement Notice

## Status

`humanvalueexchange/hanshermesagent` is retired as an implementation source.
It is preserved for historical reference, provenance, and rollback investigation
only.

## Hard rule

Do not add, modify, or extend application code, MCP servers, skills, runtime
configuration, tests, deployment files, or operational behavior in this
repository.

The only permitted future changes are narrowly scoped retirement metadata,
historical corrections, security notices, or explicitly approved rollback work.

## Active ownership

- HVE-Librarian implementation and profile work belongs in
  `humanvalueexchange/hve-librarian`.
- HVE durable knowledge and communications belong in
  `HansHWestphal/hve-knowledge-and-operations`.
- When the active owner of another Hermes component is unclear, verify the
  current repository before editing.

The repository may be read and referenced, but a local path, existing runtime
process, or historical deployment source does not authorize new commits here.
